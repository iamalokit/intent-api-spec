allOf:
  - $ref: '../common/PhoneNumberRequest.yaml'
  - type: object
    required:
      - otp
    properties:
      otp:
        type: string
        description: 6-digit OTP received via SMS
        pattern: '^\d{6}$'
        example: "482910"
