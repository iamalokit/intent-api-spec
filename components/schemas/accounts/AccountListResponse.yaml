allOf:
  - $ref: '../common/PagedResponse.yaml'
  - type: object
    required:
      - data
    properties:
      data:
        type: array
        items:
          $ref: './AccountResponse.yaml'
        description: Accounts on the current page, ordered by creation date descending
