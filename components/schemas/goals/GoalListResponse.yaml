allOf:
  - $ref: '../common/PagedResponse.yaml'
  - type: object
    required:
      - data
    properties:
      data:
        type: array
        items:
          $ref: './GoalResponse.yaml'
        description: Goals on the current page, ordered by creation date descending
