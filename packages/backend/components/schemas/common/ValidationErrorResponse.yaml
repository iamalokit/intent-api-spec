allOf:
  - $ref: './ErrorResponse.yaml'
  - type: object
    required:
      - errors
    properties:
      errors:
        type: array
        minItems: 1
        items:
          $ref: './ValidationErrorItem.yaml'
        description: One entry per invalid field, or a single `(body)` entry for a cross-field violation
