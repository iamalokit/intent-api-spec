allOf:
  - $ref: '../common/PagedResponse.yaml'
  - type: object
    required:
      - data
    properties:
      data:
        type: array
        items:
          $ref: './BudgetAuditLogEntryResponse.yaml'
        description: Audit log entries on the current page, ordered by changedAt descending
