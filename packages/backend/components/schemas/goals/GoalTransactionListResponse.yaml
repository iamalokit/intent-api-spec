allOf:
  - $ref: '../common/PagedResponse.yaml'
  - type: object
    required:
      - data
    properties:
      data:
        type: array
        items:
          $ref: './GoalTransactionResponse.yaml'
        description: Transactions on the current page, ordered by creation date descending
