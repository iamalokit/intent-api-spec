post:
  tags:
    - Auth
  summary: Verify OTP entered by the user
  description: >
    Verifies the OTP sent to the user's phone number. On success, returns
    a JWT token to be used for authenticated requests.
  operationId: verifyOtp
  security: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/auth/VerifyOtpRequest.yaml'
        example:
          phoneNumber: "+919876543210"
          otp: "482910"
  responses:
    '200':
      description: OTP verified successfully
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/auth/VerifyOtpResponse.yaml'
          example:
            message: OTP verified successfully
    '400':
      description: Invalid or expired OTP
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/common/ErrorResponse.yaml'
          example:
            status: 400
            error: Bad Request
            message: Invalid OTP. Please try again.
    '429':
      description: Too many verification attempts — rate limit exceeded
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/common/ErrorResponse.yaml'
          example:
            status: 429
            error: Too Many Requests
            message: "OTP verification limit exceeded. Please try again after 60 seconds."
    '500':
      $ref: '../../components/responses/InternalServerErrorResponse.yaml'
